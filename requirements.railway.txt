# Minimal Railway deployment dependencies
# Excludes heavy ML packages (torch, CUDA) to stay under 4GB limit

# Core
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1
typing-extensions==4.15.0

# OpenAI API
openai==2.14.0

# Document parsing (lightweight versions only)
python-docx==1.2.0
pypdfium2==4.30.0
beautifulsoup4==4.14.3
lxml==6.0.2
Pillow==11.3.0
openpyxl==3.1.5

# Docling minimal (no ML models)
docling-core==2.57.0
docling-parse==4.7.2

# HTTP/JSON
requests==2.32.5
httpx==0.28.1
